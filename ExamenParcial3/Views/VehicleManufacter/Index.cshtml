@model IEnumerable<ExamenParcial3.Models.VehicleManufacter>

@{
    ViewData["Title"] = "Index";
}

<h1>Manufacters</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>

<form asp-action="Index" method="get">
    <div class="form-actions no-color">
        <p>
            Find by manufacter: 
            <input type="text" name="SearchString" />
            <input type="submit" value="Search" /> |
            <a asp-action="Index">Back to Full List</a>
        </p>
    </div>
</form>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ManufacterID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ManufacterName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr><td>
                @Html.DisplayFor(modelItem => item.ManufacterID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ManufacterName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ManufacterID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ManufacterID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ManufacterID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
